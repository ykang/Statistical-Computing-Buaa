myFun2 =  function(y)
    {
      out = log(y)
      return(out)
    }

a[4]


myFun = function(x)
    {
        logx = myFun2(x)
        logx.length = length(logx)
        return(logx.length)
    }


myFun3  = function(x)
    {
        if(x<10)
            {
                stop("This is not allowed!")
            }


        x.max = max(x)
        return(x.max)
    }
